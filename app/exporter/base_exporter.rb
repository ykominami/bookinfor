class BaseExporter
  def initialize(vx, keys, ks)
=begin    
    @vx = vx
    # p "BaseImporter keys=#{keys}"
    @keys = keys
    @delkeys = @keys["remove"]
    @ks = ks
    @ignore_fields ||= %W()
=end
  end
end
