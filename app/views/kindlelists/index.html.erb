<p style="color: green"><%= notice %></p>

<h1>Kindlelists</h1>
<%= turbo_frame_tag "kindle_form" do %>
  <%= render "form_comp", kl: paginatex[0] %>
<% end %>

<div id="kindlelists">
  <%= render TblComponent.new(name: listx.name, header: listx.header, body: listx.body) %>

  <%= paginate paginatex %>
</div>

<%= link_to "New readinglist", new_readinglist_path %>
