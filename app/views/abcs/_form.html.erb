<%= form_with(model: abc) do |form| %>
  <% if abc.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(abc.errors.count, "error") %> prohibited this abc from being saved:</h2>

      <ul>
        <% abc.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :zid, style: "display: block" %>
    <%= form.number_field :zid %>
  </div>

  <div>
    <%= form.label :s, style: "display: block" %>
    <%= form.text_field :s %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
