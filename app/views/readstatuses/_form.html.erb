<%= form_with(model: readstatus) do |form| %>
  <% if readstatus.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(readstatus.errors.count, "error") %> prohibited this readstatus from being saved:</h2>

      <ul>
        <% readstatus.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
