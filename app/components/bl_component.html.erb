<% if @ind == 1 %>
    abc
    <turbo-frame id="b3" >
    2 turbo-frame id="b3"
    </turbo-frame>
    defx
        <div class="col-sm">@params=<%= @params %></div>
        <div class="col-sm">@ind=<%= @ind %></div>
        <div class="col-sm">@ind_next=<%= @ind_next %></div>
        <div class="col-sm"><%= @name %></div>
        <div class="col-sm"><%= @proxy.id %></div>
        <div class="col-sm"><%= @proxy.totalID %></div>
        <%= link_to icon_with_text("plus-circle", "Up"),
                    "index3/#{@ind_next}",
                    class: "btn btn-outline-primary",
                    data: { turbo_frame: "b3" }
        %>
GHI
<% else %>
    <%= helpers.turbo_stream.update id="b3" do %>
    X turbo_stream.update id="b3"
    b3
    <% end %>

<% end %>
