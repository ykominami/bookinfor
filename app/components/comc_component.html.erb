<div id="<%= dom_id @target %>">
  <div class="row py-2 border-top">
  <div class="col-4 my-auto">
    <%= @zid %>
  </div>
  <div class="col-4 my-auto">
    <%= @s %>
  </div>
  <div class="col-4">
    <div class="d-flex justify-content-end">
      <%= link_to "編集", edit_abc_path(@target), class: "btn btn-sm btn-outline-primary me-2" %>
      <%= button_to "削除", @target, method: :delete, class: "btn btn-sm btn-outline-danger" %>
    </div>
  </div>
  </div>
</div>

  
